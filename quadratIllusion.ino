
void quadrat()
{
  for(int y = 0; y <8; y++)                                  //senkrecht links    würel 1
  {
    for(int x = 0; x < 1; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 0; y <1; y++)                                 //waagerecht oben
  {
    for(int x = 0; x < 8; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 0; y <8; y++)                                 //senkrecht rechts
  {
    for(int x = 7; x < 8; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 7; y <8; y++)                                 //waagerecht unten
  {
    for(int x = 0; x < 8; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
  
 
  for(int y = 1; y <7; y++)                                 //senkrecht links   würfel 2
  {
    for(int x = 1; x < 2; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <2; y++)                                 //waagerecht oben
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <7; y++)                                 //senkrecht rechts
  {
    for(int x = 6; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 6; y <7; y++)                                 //waagerecht unten
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);

    
  for(int y = 2; y <6; y++)                                 //senkrecht links  würfel 3
  {
    for(int x = 2; x < 3; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 2; y <3; y++)                                 //waagerecht oben
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 2; y <6; y++)                                 //senkrecht rechts
  {
    for(int x = 5; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 5; y <6; y++)                                 //waagerecht unten
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
   
  for(int y = 3; y <5; y++)                                 //senkrecht links   würfel 4
  {
    for(int x = 3; x < 4; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 3; y <4; y++)                                 //waagerecht oben
  {
    for(int x = 3; x < 5; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 3; y <5; y++)                                 //senkrecht rechts
  {
    for(int x = 4; x < 5; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 4; y <5; y++)                                 //waagerecht unten
  {
    for(int x = 3; x < 5; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
     for(int y = 2; y <6; y++)                                 //senkrecht links  würfel 3
  {
    for(int x = 2; x < 3; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
    
  }
  for(int y = 2; y <3; y++)                                 //waagerecht oben
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 2; y <6; y++)                                 //senkrecht rechts
  {
    for(int x = 5; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 5; y <6; y++)                                 //waagerecht unten
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
    for(int y = 1; y <7; y++)                                 //senkrecht links   würfel 2
  {
    for(int x = 1; x < 2; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <2; y++)                                 //waagerecht oben
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <7; y++)                                 //senkrecht rechts
  {
    for(int x = 6; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 6; y <7; y++)                                 //waagerecht unten
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
    for(int y = 0; y <8; y++)                                  //senkrecht links    würel 1
  {
    for(int x = 0; x < 1; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 0; y <1; y++)                                 //waagerecht oben
  {
    for(int x = 0; x < 8; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 0; y <8; y++)                                 //senkrecht rechts
  {
    for(int x = 7; x < 8; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 7; y <8; y++)                                 //waagerecht unten
  {
    for(int x = 0; x < 8; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
  
 
  for(int y = 1; y <7; y++)                                 //senkrecht links   würfel 2
  {
    for(int x = 1; x < 2; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <2; y++)                                 //waagerecht oben
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <7; y++)                                 //senkrecht rechts
  {
    for(int x = 6; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 6; y <7; y++)                                 //waagerecht unten
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);

    
  for(int y = 2; y <6; y++)                                 //senkrecht links  würfel 3
  {
    for(int x = 2; x < 3; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 2; y <3; y++)                                 //waagerecht oben
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 2; y <6; y++)                                 //senkrecht rechts
  {
    for(int x = 5; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 5; y <6; y++)                                 //waagerecht unten
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
   
  for(int y = 3; y <5; y++)                                 //senkrecht links   würfel 4
  {
    for(int x = 3; x < 4; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 3; y <4; y++)                                 //waagerecht oben
  {
    for(int x = 3; x < 5; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 3; y <5; y++)                                 //senkrecht rechts
  {
    for(int x = 4; x < 5; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 4; y <5; y++)                                 //waagerecht unten
  {
    for(int x = 3; x < 5; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
     for(int y = 2; y <6; y++)                                 //senkrecht links  würfel 3
  {
    for(int x = 2; x < 3; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
    
  }
  for(int y = 2; y <3; y++)                                 //waagerecht oben
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 2; y <6; y++)                                 //senkrecht rechts
  {
    for(int x = 5; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 5; y <6; y++)                                 //waagerecht unten
  {
    for(int x = 2; x < 6; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
    for(int y = 1; y <7; y++)                                 //senkrecht links   würfel 2
  {
    for(int x = 1; x < 2; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <2; y++)                                 //waagerecht oben
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
  for(int y = 1; y <7; y++)                                 //senkrecht rechts
  {
    for(int x = 6; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
   for(int y = 6; y <7; y++)                                 //waagerecht unten
  {
    for(int x = 1; x < 7; x++)
    {
      matrix.drawPixel(x, y, matrix.Color(20,150,75));
      matrix.show();
    }
  }
    delay(50);
    matrix.fillScreen(0);
   
}
